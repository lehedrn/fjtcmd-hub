<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- 继承父 POM，复用版本管理和依赖管理 -->
    <parent>
        <groupId>com.fjtcmd.hub</groupId>
        <artifactId>fjtcmd-hub</artifactId>
        <version>3.9.2</version>
    </parent>

    <artifactId>fjtcmd-hub-generator-cli</artifactId>
    <packaging>jar</packaging>

    <name>fjtcmd-hub-generator-cli</name>
    <description>体彩门店智慧互动信息 代码生成 CLI 工具 - 复用 generator 模块，独立运行，无需数据库连接</description>

    <properties>
        <druid.version>1.2.28</druid.version>
        <maven-shade-plugin.version>3.6.0</maven-shade-plugin.version>
    </properties>

    <dependencies>
        <!-- 复用 generator 模块（含 GenTable / GenUtils / VelocityUtils / 模板 / common） -->
        <dependency>
            <groupId>com.fjtcmd.hub</groupId>
            <artifactId>fjtcmd-hub-generator</artifactId>
            <exclusions>
                <!-- CLI 不启动 Web / DataSource / Redis -->
                <exclusion>
                    <groupId>com.alibaba</groupId>
                    <artifactId>druid-spring-boot-4-starter</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- Druid 本体（DDL SQL 解析器需要，不含 Spring Boot starter） -->
        <dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>druid</artifactId>
            <version>${druid.version}</version>
        </dependency>

        <!-- SnakeYAML（配置文件解析） -->
        <dependency>
            <groupId>org.yaml</groupId>
            <artifactId>snakeyaml</artifactId>
        </dependency>

        <!-- 日志：SLF4J + Logback -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <!-- 打包为 fat jar -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>${maven-shade-plugin.version}</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>com.fjtcmd.hub.generator.cli.GeneratorCli</mainClass>
                                </transformer>
                            </transformers>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
