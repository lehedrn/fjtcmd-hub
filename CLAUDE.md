# CLAUDE.md

## 上下文加载

读取本文件后，必须立即读取 @docs/fjtcmd-hub/info.md，获取项目技术栈、目录结构、常用命令等信息。

## 项目概述

fjtcmd-hub（体彩门店智慧互动信息）是一个基于 RuoYi-Vue 框架二次开发的项目，面向体彩门店提供智慧互动信息化管理。项目继承若依的用户、角色、权限等基础能力，在此基础上定制业务模块。

## AI 行为偏好

### 回复风格
- 回复使用中文，代码标识符和技术术语保持英文原样
- 详细展开：复杂问题主动展示完整分析过程和细节，不需要用户追问
- 遇到不确定的地方，主动说明不确定性并列出可能的方向

### 代码风格
- 代码注释使用中文
- 遵循项目现有的 Java 编码规范（RuoYi 框架规范）
- 不过度设计，不提前引入不需要的模块或依赖（YAGNI）
- 优先编辑现有文件而非创建新文件

### 协作偏好
- **讨论优先**：方案先讨论，未经确认不执行
- **不擅自行动**：不擅自删除文件、不擅自引入新依赖、不擅自优化重构
- **主动建议**：可主动提出建议，经用户同意后推进
- **上下文连贯**：基于已有文档和结论工作，不脱离现有基础重来

### 工作习惯
- **回到根目录**：执行完命令或任务后，回到项目根目录（`/home/workspaces/com/ztq/tcmd/fjtcmd-hub`）

## 标准文档

项目已建立完整的开发规范体系，位于 `docs/standards/` 目录。

**开发新功能时，必须先查阅相关规范：**

| 规范类型 | 完整路径 | 说明 |
|---------|---------|------|
| 后端编码规范 | `docs/standards/coding/backend.md` | Java 17 + Spring Boot 4.0.3 编码规范 |
| 前端编码规范 | `docs/standards/coding/frontend.md` | Vue 3 + Vite + TypeScript 编码规范 |
| API 设计规范 | `docs/standards/api-design.md` | RESTful API 设计规范（SpringDoc） |
| Git 提交规范 | `docs/standards/commit.md` | 提交信息和分支命名规范 |
| 后端测试规范 | `docs/standards/test/backend-api.md` | curl 自动化测试规范 |
| 需求模板指南 | `docs/standards/requirement/00-模板选择指南.md` | 选择合适的需求文档模板 |
| 标准需求模板 | `docs/standards/requirement/01-标准模板.md` | CRUD/Tree/Sub 标准模板 |
| 简单业务模板 | `docs/standards/requirement/02-简单业务模板.md` | CRUD+ 业务规则模板 |

**强制要求**：
1. 开发新功能前，必须先查阅相关规范文档
2. 开发完成后，检查代码是否符合规范要求
3. 遇到不确定的地方，主动查阅规范或询问用户

## 文档规范

**更新策略：** 执行操作后主动思考是否需更新文档，如需要则按写入规范执行

**写入规范：**
- 单次写入超过 200 行时，分段完成（每次 100-200 行）
- 文档内容过大时，先按主题或模块拆分为分文档，依次写入
- 拆分后的文档通过索引文档组织，避免上下文丢失

**命名规范：**
- 文件名可以使用中文（如 `01-概述.md`）
- 所有文档存放在 `docs/` 目录下
