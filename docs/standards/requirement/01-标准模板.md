# 标准模板（CRUD/Tree/Sub）

**适用场景**：
- 单表 CRUD 操作（如学生管理、商品管理）
- 树形结构数据（如部门管理、分类管理）
- 主子表结构（如订单 - 订单明细）
- 无复杂业务流程，纯数据维护类功能

**文档篇幅**：1-2 页

**代码生成支持**：fjtcmd-hub-generator-cli 完整支持

---

## 1. 需求概述

### 1.1 基本信息

| 项目 | 说明 |
|------|------|
| 模块名称 | {模块名}，如 `学生管理` |
| 模块编码 | `{module}.{business}`，如 `demo.student` |
| 文档版本 | 1.0 |
| 创建日期 | YYYY-MM-DD |
| 最后更新 | YYYY-MM-DD |
| 负责人 | {姓名/工号} |
| 优先级 | P0-高 / P1-中 / P2-低 |

### 1.2 需求背景

**业务目标**：
- 描述本模块要解决的业务问题
- 例：实现学生信息的规范化、信息化管理，提高教务管理效率

**用户痛点**（可选）：
- 描述当前存在的问题
- 例：现有手工记录方式效率低、易出错、难以统计

**预期价值**：
- 描述功能完成后的收益
- 例：减少人工录入时间 80%，数据准确率提升至 99%

### 1.3 功能范围

**本模块包含的功能**：
- [ ] 数据列表查询（分页、筛选、排序）
- [ ] 新增数据
- [ ] 修改数据
- [ ] 删除数据（软删除）
- [ ] 数据导出（Excel）
- [ ] 数据导入（批量导入）
- [ ] 其他：{自定义功能}

**本模块不包含的功能**：
- {明确说明不做的功能，避免范围蔓延}

### 1.4 用户角色

| 角色 | 职责 | 典型操作 |
|------|------|---------|
| {角色 1} | {职责描述} | 查询、新增、修改、删除 |
| {角色 2} | {职责描述} | 仅查询、导出 |
| {角色 3} | {职责描述} | 全部操作 |

### 1.5 使用场景

**场景 1：{场景名称}**
- **触发条件**：{什么情况下触发}
- **参与角色**：{谁参与}
- **操作流程**：{简述操作步骤}
- **预期结果**：{期望达成的结果}

### 1.6 名词定义

| 名词 | 定义/说明 |
|------|----------|
| {专业术语} | {解释} |
| {业务词汇} | {解释} |

---

## 2. 表结构 (DDL)

### 2.1 单表 CRUD 模板

```sql
-- 表名格式：{业务前缀}_{表名}，如 demo_student
CREATE TABLE {table_name} (
  id              BIGINT          NOT NULL      AUTO_INCREMENT COMMENT '编号',
  -- 业务字段定义
  {field_name}    {field_type}    {constraints} COMMENT '{注释}',
  -- 审计字段（必须）
  create_time     DATETIME        DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time     DATETIME        DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  create_by       VARCHAR(64)     DEFAULT ''    COMMENT '创建人',
  update_by       VARCHAR(64)     DEFAULT ''    COMMENT '更新人',
  remark          VARCHAR(500)    DEFAULT ''    COMMENT '备注',
  del_flag        CHAR(1)         DEFAULT '0'   COMMENT '删除标志（0 正常 1 删除）',
  -- 索引定义
  PRIMARY KEY (id),
  KEY idx_{field} ({field}) COMMENT '{索引说明}'
) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=utf8mb4 COMMENT='{表注释}';
```

### 2.2 树表模板（Tree）

**适用场景**：部门管理、商品分类、菜单管理等具有层级关系的数据

**必须包含的字段**：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `id` | BIGINT | 主键（树编码） |
| `parent_id` | BIGINT | 父节点 ID（树父编码） |
| `ancestors` | VARCHAR(500) | 祖级列表 |
| `{name 字段}` | VARCHAR | 树名称字段 |

```sql
CREATE TABLE demo_category (
  id              BIGINT          NOT NULL      AUTO_INCREMENT COMMENT '类别 ID',
  parent_id       BIGINT          DEFAULT 0     COMMENT '父类别 ID',
  ancestors       VARCHAR(500)    DEFAULT ''    COMMENT '祖级列表',
  category_name   VARCHAR(30)     NOT NULL      COMMENT '类别名称',
  sort            INT             DEFAULT 0     COMMENT '排序',
  status          CHAR(1)         DEFAULT '0'   COMMENT '状态（0 正常 1 停用）',
  create_time     DATETIME        DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time     DATETIME        DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  create_by       VARCHAR(64)     DEFAULT ''    COMMENT '创建人',
  update_by       VARCHAR(64)     DEFAULT ''    COMMENT '更新人',
  del_flag        CHAR(1)         DEFAULT '0'   COMMENT '删除标志',
  PRIMARY KEY (id),
  KEY idx_parent_id (parent_id)
) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=utf8mb4 COMMENT='产品类别表';
```

### 2.3 主子表模板（Sub）

**适用场景**：订单 - 订单明细、客户 - 商品、文章 - 评论等一对多关系

**主表结构**：
```sql
CREATE TABLE demo_customer (
  id              BIGINT          NOT NULL      AUTO_INCREMENT COMMENT '客户 ID',
  customer_name   VARCHAR(100)    NOT NULL      COMMENT '客户名称',
  phone           VARCHAR(20)                   COMMENT '联系电话',
  create_time     DATETIME        DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time     DATETIME        DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (id)
) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=utf8mb4 COMMENT='客户表';
```

**子表结构**：
```sql
CREATE TABLE demo_goods (
  id              BIGINT          NOT NULL      AUTO_INCREMENT COMMENT '商品 ID',
  customer_id     BIGINT          NOT NULL      COMMENT '客户 ID（外键）',
  goods_name      VARCHAR(100)    NOT NULL      COMMENT '商品名称',
  price           DECIMAL(10,2)   NOT NULL      COMMENT '商品价格',
  stock           INT             DEFAULT 0     COMMENT '库存数量',
  create_time     DATETIME        DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time     DATETIME        DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (id),
  KEY idx_customer_id (customer_id)
) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=utf8mb4 COMMENT='商品表';
```

---

## 3. 代码生成配置 (YAML)

### 3.1 单表配置示例

```yaml
# config.yaml
global:
  packageName: com.fjtcmd.hub
  author: fjtcmd
  moduleName: demo
  businessName: student
  functionName: 学生管理
  parentMenuId: 0

table:
  tableName: demo_student
  tableComment: 学生信息表
  pkColumn: id
  treeCode: ""
  treeParentCode: ""
  treeName: ""

columns:
  - columnName: student_name
    columnType: VARCHAR(50)
    columnComment: 学生名称
    javaType: String
    javaField: studentName
    isInsert: true
    isEdit: true
    isList: true
    isQuery: true
    queryType: LIKE
    htmlType: input
    dictType: ""
  
  - columnName: student_age
    columnType: INT
    columnComment: 年龄
    javaType: Integer
    javaField: studentAge
    isInsert: true
    isEdit: true
    isList: true
    isQuery: false
    queryType: EQ
    htmlType: input
    dictType: ""
  
  - columnName: student_sex
    columnType: CHAR(1)
    columnComment: 性别
    javaType: String
    javaField: studentSex
    isInsert: true
    isEdit: true
    isList: true
    isQuery: true
    queryType: EQ
    htmlType: select
    dictType: sys_user_sex
  
  - columnName: student_status
    columnType: CHAR(1)
    columnComment: 状态
    javaType: String
    javaField: studentStatus
    isInsert: true
    isEdit: true
    isList: true
    isQuery: true
    queryType: EQ
    htmlType: radio
    dictType: sys_normal_disable
```

### 3.2 树表配置示例

```yaml
table:
  tableName: demo_category
  tableComment: 产品类别表
  pkColumn: id
  treeCode: id
  treeParentCode: parentId
  treeName: categoryName

columns:
  - columnName: category_name
    columnType: VARCHAR(30)
    columnComment: 类别名称
    javaType: String
    javaField: categoryName
    isInsert: true
    isEdit: true
    isList: true
    isQuery: true
    queryType: LIKE
    htmlType: input
    dictType: ""
  
  - columnName: parent_id
    columnType: BIGINT
    columnComment: 父类别 ID
    javaType: Long
    javaField: parentId
    isInsert: true
    isEdit: true
    isList: true
    isQuery: false
    queryType: EQ
    htmlType: select
    dictType: ""
```

### 3.3 主子表配置示例

**主表配置**：
```yaml
table:
  tableName: demo_customer
  tableComment: 客户表
  pkColumn: id
  subTableName: demo_goods
  subTableFkName: customer_id

columns:
  - columnName: customer_name
    columnType: VARCHAR(100)
    columnComment: 客户名称
    javaType: String
    javaField: customerName
    isInsert: true
    isEdit: true
    isList: true
    isQuery: true
    queryType: LIKE
    htmlType: input
    dictType: ""
```

**子表配置**：
```yaml
subColumns:
  - columnName: goods_name
    columnType: VARCHAR(100)
    columnComment: 商品名称
    javaType: String
    javaField: goodsName
    isInsert: true
    isEdit: true
    isList: true
    isQuery: false
    queryType: EQ
    htmlType: input
    dictType: ""
  
  - columnName: price
    columnType: DECIMAL(10,2)
    columnComment: 商品价格
    javaType: BigDecimal
    javaField: price
    isInsert: true
    isEdit: true
    isList: true
    isQuery: false
    queryType: EQ
    htmlType: input
    dictType: ""
```

---

## 4. 字典设计

### 4.1 字典类型定义

```sql
-- 字典类型
INSERT INTO sys_dict_type (dict_name, dict_type, status, create_by, create_time, remark)
VALUES ('{字典名称}', '{dict_type}', '0', 'admin', NOW(), '{备注}');

-- 字典数据
INSERT INTO sys_dict_data (dict_sort, dict_label, dict_value, dict_type, status, create_by, create_time, remark)
VALUES 
(1, '{标签 1}', '{值 1}', '{dict_type}', '0', 'admin', NOW(), '{备注}'),
(2, '{标签 2}', '{值 2}', '{dict_type}', '0', 'admin', NOW(), '{备注}'),
(3, '{标签 3}', '{值 3}', '{dict_type}', '0', 'admin', NOW(), '{备注}');
```

### 4.2 常用字典示例

```sql
-- 性别字典
INSERT INTO sys_dict_type (dict_name, dict_type, status, create_by, create_time, remark)
VALUES ('用户性别', 'sys_user_sex', '0', 'admin', NOW(), '用户性别列表');

INSERT INTO sys_dict_data (dict_sort, dict_label, dict_value, dict_type, status, create_by, create_time, remark)
VALUES 
(1, '男', '0', 'sys_user_sex', '0', 'admin', NOW(), '性别男'),
(2, '女', '1', 'sys_user_sex', '0', 'admin', NOW(), '性别女'),
(3, '未知', '2', 'sys_user_sex', '0', 'admin', NOW(), '性别未知');

-- 状态字典
INSERT INTO sys_dict_type (dict_name, dict_type, status, create_by, create_time, remark)
VALUES ('系统开关', 'sys_normal_disable', '0', 'admin', NOW(), '系统开关列表');

INSERT INTO sys_dict_data (dict_sort, dict_label, dict_value, dict_type, status, create_by, create_time, remark)
VALUES 
(1, '正常', '0', 'sys_normal_disable', '0', 'admin', NOW(), '正常状态'),
(2, '停用', '1', 'sys_normal_disable', '0', 'admin', NOW(), '停用状态');
```

---

## 5. 功能清单

### 5.1 功能列表

| 功能名称 | 功能编码 | 权限标识 | 说明 |
|---------|---------|---------|------|
| 查询列表 | {module}:{business}:list | {module}:{business}:list | 分页查询数据列表 |
| 查询详情 | {module}:{business}:query | {module}:{business}:query | 查询单条数据详情 |
| 新增数据 | {module}:{business}:add | {module}:{business}:add | 新增数据 |
| 修改数据 | {module}:{business}:edit | {module}:{business}:edit | 修改数据 |
| 删除数据 | {module}:{business}:remove | {module}:{business}:remove | 删除数据（支持批量） |
| 导出数据 | {module}:{business}:export | {module}:{business}:export | 导出 Excel |
| 导入数据 | {module}:{business}:import | {module}:{business}:import | 批量导入 Excel |

### 5.2 菜单 SQL

```sql
-- 主菜单
INSERT INTO sys_menu (menu_name, parent_id, order_num, path, component, is_frame, is_cache, menu_type, visible, status, perms, icon, create_by, create_time, remark)
VALUES ('{模块名称}', {parent_id}, 1, '{business}', '{module}/{business}/index', 1, 0, 'C', '0', '0', '{module}:{business}:list', '#', 'admin', NOW(), '{模块名称}菜单');

-- 查询按钮
INSERT INTO sys_menu (menu_name, parent_id, order_num, path, component, is_frame, is_cache, menu_type, visible, status, perms, icon, create_by, create_time, remark)
VALUES ('{模块名称}查询', @parentId, 1, '#', '', 1, 0, 'F', '0', '0', '{module}:{business}:query', '#', 'admin', NOW(), '');

-- 新增按钮
INSERT INTO sys_menu (menu_name, parent_id, order_num, path, component, is_frame, is_cache, menu_type, visible, status, perms, icon, create_by, create_time, remark)
VALUES ('{模块名称}新增', @parentId, 2, '#', '', 1, 0, 'F', '0', '0', '{module}:{business}:add', '#', 'admin', NOW(), '');

-- 修改按钮
INSERT INTO sys_menu (menu_name, parent_id, order_num, path, component, is_frame, is_cache, menu_type, visible, status, perms, icon, create_by, create_time, remark)
VALUES ('{模块名称}修改', @parentId, 3, '#', '', 1, 0, 'F', '0', '0', '{module}:{business}:edit', '#', 'admin', NOW(), '');

-- 删除按钮
INSERT INTO sys_menu (menu_name, parent_id, order_num, path, component, is_frame, is_cache, menu_type, visible, status, perms, icon, create_by, create_time, remark)
VALUES ('{模块名称}删除', @parentId, 4, '#', '', 1, 0, 'F', '0', '0', '{module}:{business}:remove', '#', 'admin', NOW(), '');

-- 导出按钮
INSERT INTO sys_menu (menu_name, parent_id, order_num, path, component, is_frame, is_cache, menu_type, visible, status, perms, icon, create_by, create_time, remark)
VALUES ('{模块名称}导出', @parentId, 5, '#', '', 1, 0, 'F', '0', '0', '{module}:{business}:export', '#', 'admin', NOW(), '');
```

---

## 6. 交付物清单

| 交付物 | 格式 | 说明 |
|-------|------|------|
| DDL SQL 脚本 | .sql | 表结构定义 |
| 代码生成配置 | .yaml | fjtcmd-hub-generator-cli 配置 |
| 字典数据 SQL | .sql | 字典类型和字典数据 |
| 菜单 SQL | .sql | 菜单和按钮权限 |
| 需求文档 | .md | 本文档 |

---

## 7. 代码生成步骤

### 7.1 准备 SQL 文件

```sql
-- schema.sql
CREATE TABLE demo_student (
  id              BIGINT          NOT NULL      AUTO_INCREMENT COMMENT '编号',
  student_name    VARCHAR(50)     NOT NULL      COMMENT '学生名称',
  student_age     INT             DEFAULT 0     COMMENT '年龄',
  student_sex     CHAR(1)         DEFAULT '0'   COMMENT '性别（0 男 1 女 2 未知）',
  student_status  CHAR(1)         DEFAULT '0'   COMMENT '状态（0 正常 1 停用）',
  student_birthday DATE                          COMMENT '生日',
  create_time     DATETIME        DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time     DATETIME        DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  create_by       VARCHAR(64)     DEFAULT ''    COMMENT '创建人',
  update_by       VARCHAR(64)     DEFAULT ''    COMMENT '更新人',
  remark          VARCHAR(500)    DEFAULT ''    COMMENT '备注',
  del_flag        CHAR(1)         DEFAULT '0'   COMMENT '删除标志',
  PRIMARY KEY (id)
) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=utf8mb4 COMMENT='学生信息表';
```

### 7.2 执行代码生成

```bash
# 使用 CLI 工具
java -jar fjtcmd-hub-generator-cli/target/fjtcmd-hub-generator-cli.jar \
  --config config.yaml \
  --sql schema.sql

# 生成的文件位置：
# - Controller: fjtcmd-hub-admin/src/main/java/com/fjtcmd/hub/web/controller/demo/
# - Service: fjtcmd-hub-system/src/main/java/com/fjtcmd/hub/system/service/
# - Mapper: fjtcmd-hub-system/src/main/java/com/fjtcmd/hub/system/mapper/
# - 前端 API: fjtcmd-hub-ui/src/api/demo/
# - 前端页面: fjtcmd-hub-ui/src/views/demo/
```

### 7.3 导入菜单 SQL

```bash
# 在数据库中执行菜单 SQL
mysql -u root -p fjtcmd_hub < menu.sql
```

### 7.4 验证功能

1. 重启后端服务
2. 刷新前端页面
3. 检查菜单是否显示
4. 测试增删改查功能

---

## 8. 注意事项

1. **表名规范**：使用业务前缀（如 demo_、sys_）
2. **字段命名**：使用下划线命名法（snake_case）
3. **审计字段**：必须包含 create_time、update_time、create_by、update_by、remark、del_flag
4. **索引优化**：为常用查询字段添加索引
5. **字典设计**：尽量使用字典管理状态、类型等枚举值
6. **权限标识**：遵循 {module}:{business}:{action} 格式

---

**文档版本**: 1.0  
**最后更新**: 2026-06-11  
**基于版本**: fjtcmd-hub (Spring Boot 4.0.3)
